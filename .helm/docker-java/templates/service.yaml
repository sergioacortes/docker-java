apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker-java.fullname" . }}
  labels:
    {{- include "docker-java.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docker-java.selectorLabels" . | nindent 4 }}
