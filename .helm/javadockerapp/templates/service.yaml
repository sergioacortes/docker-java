apiVersion: v1
kind: Service
metadata:
  name: {{ include "javadockerapp.fullname" . }}
  labels:
    {{- include "javadockerapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "javadockerapp.selectorLabels" . | nindent 4 }}
